import Nimble
import Quick
@testable import Til

class TilAppSpec: QuickSpec {
  override func spec() {

    describe("test suite") {

      it("runs tests") {
        expect(true).to(beTrue())
      }
    }
  }
}
